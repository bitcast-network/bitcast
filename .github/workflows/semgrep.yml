name: Semgrep SAST

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  semgrep:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo hello
